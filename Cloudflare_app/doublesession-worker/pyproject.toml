[project]
name = "doublesession-worker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "webtypy>=0.1.7",
    "fastapi",
    "jinja2",
    "python-multipart",
]

[dependency-groups]
dev = [
    "workers-py"
]
